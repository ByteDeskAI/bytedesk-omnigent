"""Hermes harness package.

ByteDesk-owned omnigent harness that drives a local Hermes Agent over the
Agent Client Protocol (ACP, protocolVersion 1) via the ``hermes acp`` CLI
(stdin/stdout), mirroring the upstream ``grok-native`` ACP bridge.
"""
